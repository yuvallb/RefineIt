/** Application-wide constants. */
export const APP_NAME = 'Transform Studio';

export const SCHEMA_VERSION = 1 as const;

/** Maximum preview rows returned from the worker (see plan/04-execution-engine.md). */
export const PREVIEW_ROW_CAP = 100;
